# The improvement loop

**The runnable checklist is `~/projects/softmax/improvement-loop.md`.** Part 1 is the
portable practice — one pass end to end, with a "done when" per step and the two human
gates. Part 2 is the crewborg instantiation: the actual commands, paths and tools.

It sits outside this repo for the same reason the working context does; see
[`WORKING_CONTEXT.md`](WORKING_CONTEXT.md) and `~/projects/softmax/AGENTS.md`.

The measurement disciplines these steps assume are in
[`best_practices.md`](best_practices.md), which is still local to this player.
